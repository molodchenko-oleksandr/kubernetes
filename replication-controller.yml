apiVersion: apps/v1
kind: ReplicaSet

metadata:
  name: kuber-rs-1

spec:
  replicas: 3

  selector:
    matchLabels:
      env: dev

  template:
    metadata:
      labels:
        env: dev
    spec:
      containers:
        - name: kuber-app
          image: nginx:alpine
